# Copy to .env and fill in whichever keys you have.
# The eval engine will run every model whose key is set; missing keys are skipped.
# With NO keys set, it falls back to a deterministic Mock provider so the pipeline still runs end-to-end.

GROQ_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
